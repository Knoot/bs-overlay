services:
  overlay:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "${FRONTEND_PORT:-4200}:4200"
    volumes:
      - ./:/app
      - node_modules:/app/node_modules
    init: true
    restart: unless-stopped

volumes:
  node_modules:
